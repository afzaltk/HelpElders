@model NewModel
@Styles.Render("~/Content/quizStyle.css")

@{
    ViewBag.Title = "Result";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Questionnaire Result</h2>






<div style="font-size:30px">
    Based on the futher answers that you gave, there is a @ViewBag.Priority likelihood that you are
    <div class="answerArea span" style="font:bold;font-size:40px;color:red; justify-content: center;text-align:center">@ViewBag.AbuseType</div>
</div>
<div style="font-size:30px">
    Click below to read more about Elder abuse. You can read and understand the different types of abuses, the signs shown for each abuse etc

  
</div>
@*@using (Html.BeginForm("Postcode", "Questions", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4></h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })


        <div class="form-group">
            <div class="questionArea">
                <hr />
                <div>
                </div>
                <div class="question">
                    <span>  @Html.DisplayFor(newModel => newModel.responseTypes.Postcode)</span>
                </div>
            </div>

        </div>

        @Html.HiddenFor(newModel => newModel.responses.QuestionId, new { Value = @Model.questions.QuestionId })
        <div class="form-group question questionArea">

            @Html.EditorFor(newModel => newModel.responseTypes.Postcode, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(newModel => newModel.responseTypes.Postcode, "", new { @class = "text-danger" })*@
            @*<span>
                <label> @Html.RadioButtonFor(newModel => newModel.responses.Answer, "1", new { @class = "class_radio" }) Yes</label>   &nbsp &nbsp &nbsp
                <label> @Html.RadioButtonFor(newModel => newModel.responses.Answer, "0", new { @class = "class_radio" }) No</label>
            </span>*@

        @*</div>

        <div class="form-group ">
            <div class="btn_area">
                <input type="submit" value="Submit" class="btn btn-success" /><br />

            </div>
        </div>
    </div>
}*@

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
